var n=4;
for(var i = 0;i < n; i++){
    lines = read_line().split(',');
    for(var j=0;j<lines.length;j++){
    	var a = lines[0];
    }
}
var brr = a.split(',');
var arr = [10065,10099,10579];
if(arr.includes(brr[0]) == -1 || arr.includes(brr[1]) == -1){
	print('无法获得有效线路');
}